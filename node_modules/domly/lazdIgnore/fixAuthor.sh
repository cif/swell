git filter-branch --commit-filter '
        if [ "$GIT_COMMITTER_EMAIL" = "lawdavis@adobe.com" ];
        then
                GIT_COMMITTER_EMAIL="lazdnet@gmail.com";
                GIT_AUTHOR_EMAIL="lazdnet@gmail.com";
                git commit-tree "$@";
        else
                git commit-tree "$@";
        fi' HEAD